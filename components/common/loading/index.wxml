<!-- loading/index.wxml -->
<view wx:if="{{ show }}" class="loading{{ fullscreen?' loading-fullscreen':'' }}">
  <view class="tips" wx:if="{{ tips }}">{{ tips }}</view>
  <slot wx:else></slot>
</view>